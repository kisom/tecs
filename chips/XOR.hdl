/**
  * basic XOR chip constructed entirely of NAND gates
 */

CHIP XOR {
    IN a, b;
    OUT out;

    PARTS:
    NAND(a=a, b=b, out=nota);
    NAND(a=b, b=b, out=notb);
    NAND(a=a, b=notb, out=nand1);
    NAND(a=b, b=nota, out=nand2);
    NAND(a=nand1, b=nand2, out=out);
}

